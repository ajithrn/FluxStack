/**
 * Block Editor Scripts
 *
 * Compiled by Vite and injected into the block editor by Sage.
 *
 * - Import .jsx block modules here (only for complex blocks needing custom editor UI)
 * - Register block styles and variants
 * - Simple blocks use PHP-only autoRegister (no JS needed)
 */

import domReady from '@wordpress/dom-ready';

// Import JSX blocks (only blocks that need media upload, InnerBlocks, etc.)
// Example: import '../../modules/hero-block/editor.jsx';

domReady(() => {
  // Block styles, unregister unwanted blocks, etc.
});
